<h1>ScooterBay Users</h1>

<ul>
    <% @users.each do |user| %>
    <li>
    <!-- This query retrieves the username attribute of the currently referenced instance of the User model -->
        <%= link_to user.username, user %>
    </li>
    <% end %>
</ul>
